# Copy this file to a local env file (for example: apps/web/.env.local).
# Never commit real secrets, private keys, mnemonics, or seeds.

NODE_ENV=development

# XRPL network config
XRPL_NETWORK=testnet
XRPL_RPC_URL=https://s.altnet.rippletest.net:51234

# Security default: if checks are missing, unavailable, or fail, deny sensitive actions.
GUARDRAILS_FAIL_CLOSED=true

# Demo bypass toggle (DEMO ONLY - never enable in production)
# Set to true to allow users to bypass guardrails checks in demo mode.
# This is automatically disabled when NODE_ENV=production.
NEXT_PUBLIC_DEMO_ALLOW_BYPASS=false

# Optional server-side credentials (leave empty unless needed).
# Never expose these as NEXT_PUBLIC_* and never persist seeds in browser storage.
XRPL_WALLET_SEED=
XRPL_PRIVATE_KEY=

